

// api  for adding doctor

const addDoctor = async(req,res)=>{
    try {
        const { name, email,password, speciality, degree, exprerience,about, fees,address}=req.body
        
    } catch (error) {
        
    }
}